import React, { useState } from 'react';
import { Sparkles, Image as ImageIcon, Type, FolderOpen, Settings, Home, Download, Copy, RefreshCw, Heart, Trash2, Search, Filter, Plus, Check, ChevronRight, Zap, TrendingUp, Package, Users, Bell, ChevronDown } from 'lucide-react';

const PODPlatform = () => {
  const [currentPage, setCurrentPage] = useState('dashboard');
  const [selectedEngine, setSelectedEngine] = useState('gpt-image-2');
  const [selectedProduct, setSelectedProduct] = useState('tshirt');
  const [selectedStyle, setSelectedStyle] = useState('cartoon');
  const [prompt, setPrompt] = useState('');
  const [isGenerating, setIsGenerating] = useState(false);
  const [generatedImages, setGeneratedImages] = useState([]);
  const [titlePrompt, setTitlePrompt] = useState('');
  const [generatedTitles, setGeneratedTitles] = useState([]);
  const [isGeneratingTitles, setIsGeneratingTitles] = useState(false);
  const [selectedMarket, setSelectedMarket] = useState('us');
  const [imageCount, setImageCount] = useState(4);
  const [titleCount, setTitleCount] = useState(10);
  const [savedItems, setSavedItems] = useState([1, 3, 5]);

  const handleGenerateImages = () => {
    setIsGenerating(true);
    setTimeout(() => {
      const newImages = Array.from({ length: imageCount }, (_, i) => ({
        id: Date.now() + i,
        url: `/api/placeholder/300/300`,
        prompt: prompt || '示例图案',
        timestamp: new Date().toLocaleString('zh-CN')
      }));
      setGeneratedImages(newImages);
      setIsGenerating(false);
    }, 1500);
  };

  const handleGenerateTitles = () => {
    setIsGeneratingTitles(true);
    setTimeout(() => {
      const sampleTitles = [
        'Funny Surfing Dog Graphic T-Shirt - Cool Beach Vibes Tee',
        'Cute Shiba Inu Sunglasses Tee - Summer Vacation Shirt',
        'Retro Style Surfing Puppy T-Shirt - Vintage Beach Apparel',
        'Hawaiian Dog Surfer Shirt - Tropical Paradise Tee',
        'Cool Dog with Shades T-Shirt - Funny Pet Lover Gift',
        'Beach Dog Graphic Tee - Ocean Waves Surfing Shirt',
        'Vintage Surfer Dog T-Shirt - California Beach Style',
        'Funny Pet Surfing T-Shirt - Cute Animal Lover Gift',
        'Tropical Dog Tee - Summer Beach Party Shirt',
        'Retro Dog Surfer Graphic Tee - 80s Style Beach Shirt'
      ];
      setGeneratedTitles(sampleTitles.slice(0, titleCount).map((t, i) => ({
        id: i,
        text: t,
        copied: false
      })));
      setIsGeneratingTitles(false);
    }, 1200);
  };

  const products = [
    { id: 'tshirt', name: 'T恤', icon: '👕' },
    { id: 'mug', name: '马克杯', icon: '☕' },
    { id: 'phonecase', name: '手机壳', icon: '📱' },
    { id: 'bag', name: '帆布包', icon: '👜' },
    { id: 'pillow', name: '抱枕', icon: '🛏️' },
    { id: 'hoodie', name: '卫衣', icon: '🧥' }
  ];

  const styles = [
    { id: 'cartoon', name: '卡通' },
    { id: 'realistic', name: '写实' },
    { id: 'minimal', name: '极简' },
    { id: 'vintage', name: '复古' },
    { id: 'cyberpunk', name: '赛博朋克' },
    { id: 'watercolor', name: '水彩' }
  ];

  const engines = [
    { id: 'gpt-image-2', name: 'GPT-Image-2', tag: '推荐' },
    { id: 'flux-pro', name: 'Flux Pro', tag: '高质量' },
    { id: 'nano-banana', name: 'Nano Banana', tag: '速度快' },
    { id: 'sd-xl', name: 'Stable Diffusion XL', tag: '经济' }
  ];

  const renderSidebar = () => (
    <div className="w-60 bg-slate-900 text-white h-screen flex flex-col">
      <div className="p-6 border-b border-slate-800">
        <div className="flex items-center gap-2">
          <div className="w-8 h-8 bg-gradient-to-br from-purple-500 to-pink-500 rounded-lg flex items-center justify-center">
            <Sparkles className="w-5 h-5" />
          </div>
          <div>
            <div className="font-bold">POD AI Studio</div>
            <div className="text-xs text-slate-400">内部工作台 v1.0</div>
          </div>
        </div>
      </div>
      <nav className="flex-1 p-4 space-y-1">
        {[
          { id: 'dashboard', icon: Home, label: '工作台' },
          { id: 'image', icon: ImageIcon, label: '图案生成' },
          { id: 'title', icon: Type, label: '标题生成' },
          { id: 'library', icon: FolderOpen, label: '素材库' },
          { id: 'settings', icon: Settings, label: '系统设置' }
        ].map(item => (
          <button
            key={item.id}
            onClick={() => setCurrentPage(item.id)}
            className={`w-full flex items-center gap-3 px-3 py-2 rounded-lg text-sm transition-colors ${
              currentPage === item.id 
                ? 'bg-purple-600 text-white' 
                : 'text-slate-300 hover:bg-slate-800'
            }`}
          >
            <item.icon className="w-4 h-4" />
            {item.label}
          </button>
        ))}
      </nav>
      <div className="p-4 border-t border-slate-800">
        <div className="bg-slate-800 rounded-lg p-3">
          <div className="text-xs text-slate-400 mb-1">本月API消耗</div>
          <div className="text-lg font-bold">¥2,847</div>
          <div className="w-full bg-slate-700 rounded-full h-1.5 mt-2">
            <div className="bg-purple-500 h-1.5 rounded-full w-2/3"></div>
          </div>
          <div className="text-xs text-slate-400 mt-1">预算 ¥5,000</div>
        </div>
      </div>
    </div>
  );

  const renderDashboard = () => (
    <div className="p-8">
      <div className="mb-8">
        <h1 className="text-2xl font-bold text-slate-800">工作台</h1>
        <p className="text-slate-500 mt-1">早上好，今天准备生成什么爆款？</p>
      </div>

      <div className="grid grid-cols-4 gap-4 mb-8">
        {[
          { label: '今日生成图案', value: '156', change: '+23%', icon: ImageIcon, color: 'purple' },
          { label: '今日生成标题', value: '420', change: '+18%', icon: Type, color: 'pink' },
          { label: '素材库总数', value: '3,287', change: '+5%', icon: FolderOpen, color: 'blue' },
          { label: '已上架产品', value: '892', change: '+12%', icon: Package, color: 'green' }
        ].map((stat, i) => (
          <div key={i} className="bg-white rounded-xl p-5 border border-slate-200">
            <div className="flex items-center justify-between mb-3">
              <div className={`w-10 h-10 rounded-lg bg-${stat.color}-100 flex items-center justify-center`}>
                <stat.icon className={`w-5 h-5 text-${stat.color}-600`} />
              </div>
              <span className="text-xs text-green-600 font-medium">{stat.change}</span>
            </div>
            <div className="text-2xl font-bold text-slate-800">{stat.value}</div>
            <div className="text-sm text-slate-500 mt-1">{stat.label}</div>
          </div>
        ))}
      </div>

      <div className="grid grid-cols-2 gap-6 mb-8">
        <div 
          onClick={() => setCurrentPage('image')}
          className="bg-gradient-to-br from-purple-500 to-pink-500 rounded-2xl p-6 text-white cursor-pointer hover:shadow-xl transition-shadow"
        >
          <ImageIcon className="w-10 h-10 mb-4" />
          <h3 className="text-xl font-bold mb-2">图案生成</h3>
          <p className="text-purple-100 text-sm mb-4">使用AI快速生成产品图案，支持多种风格和产品类型</p>
          <div className="flex items-center gap-2 text-sm font-medium">
            立即开始 <ChevronRight className="w-4 h-4" />
          </div>
        </div>
        <div 
          onClick={() => setCurrentPage('title')}
          className="bg-gradient-to-br from-blue-500 to-cyan-500 rounded-2xl p-6 text-white cursor-pointer hover:shadow-xl transition-shadow"
        >
          <Type className="w-10 h-10 mb-4" />
          <h3 className="text-xl font-bold mb-2">标题生成</h3>
          <p className="text-blue-100 text-sm mb-4">批量生成符合海外平台SEO规则的产品标题</p>
          <div className="flex items-center gap-2 text-sm font-medium">
            立即开始 <ChevronRight className="w-4 h-4" />
          </div>
        </div>
      </div>

      <div className="bg-white rounded-xl border border-slate-200 p-6">
        <div className="flex items-center justify-between mb-4">
          <h3 className="font-bold text-slate-800">最近生成记录</h3>
          <button className="text-sm text-purple-600">查看全部</button>
        </div>
        <div className="space-y-3">
          {[
            { type: 'image', text: 'Surfing dog with sunglasses', time: '5分钟前', user: '张三' },
            { type: 'title', text: '为T恤生成了10条标题', time: '12分钟前', user: '李四' },
            { type: 'image', text: 'Vintage retro coffee mug design', time: '28分钟前', user: '张三' },
            { type: 'title', text: '为马克杯生成了5条标题', time: '1小时前', user: '王五' }
          ].map((item, i) => (
            <div key={i} className="flex items-center gap-3 p-3 hover:bg-slate-50 rounded-lg">
              <div className={`w-8 h-8 rounded-lg flex items-center justify-center ${
                item.type === 'image' ? 'bg-purple-100 text-purple-600' : 'bg-blue-100 text-blue-600'
              }`}>
                {item.type === 'image' ? <ImageIcon className="w-4 h-4" /> : <Type className="w-4 h-4" />}
              </div>
              <div className="flex-1">
                <div className="text-sm font-medium text-slate-800">{item.text}</div>
                <div className="text-xs text-slate-500">{item.user} · {item.time}</div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );

  const renderImageGeneration = () => (
    <div className="p-8">
      <div className="mb-6">
        <h1 className="text-2xl font-bold text-slate-800">图案生成</h1>
        <p className="text-slate-500 mt-1">使用AI生成产品定制图案</p>
      </div>

      <div className="grid grid-cols-3 gap-6">
        <div className="col-span-1 bg-white rounded-xl border border-slate-200 p-6 h-fit">
          <h3 className="font-bold text-slate-800 mb-4">生成参数</h3>
          
          <div className="mb-5">
            <label className="text-sm font-medium text-slate-700 mb-2 block">产品类型</label>
            <div className="grid grid-cols-3 gap-2">
              {products.map(p => (
                <button
                  key={p.id}
                  onClick={() => setSelectedProduct(p.id)}
                  className={`p-3 rounded-lg border text-sm transition-all ${
                    selectedProduct === p.id
                      ? 'border-purple-500 bg-purple-50 text-purple-700'
                      : 'border-slate-200 hover:border-slate-300'
                  }`}
                >
                  <div className="text-xl mb-1">{p.icon}</div>
                  {p.name}
                </button>
              ))}
            </div>
          </div>

          <div className="mb-5">
            <label className="text-sm font-medium text-slate-700 mb-2 block">AI引擎</label>
            <div className="space-y-2">
              {engines.map(e => (
                <button
                  key={e.id}
                  onClick={() => setSelectedEngine(e.id)}
                  className={`w-full flex items-center justify-between p-3 rounded-lg border text-sm transition-all ${
                    selectedEngine === e.id
                      ? 'border-purple-500 bg-purple-50'
                      : 'border-slate-200 hover:border-slate-300'
                  }`}
                >
                  <span className="font-medium text-slate-800">{e.name}</span>
                  <span className="text-xs px-2 py-0.5 bg-slate-100 text-slate-600 rounded">{e.tag}</span>
                </button>
              ))}
            </div>
          </div>

          <div className="mb-5">
            <label className="text-sm font-medium text-slate-700 mb-2 block">风格</label>
            <div className="flex flex-wrap gap-2">
              {styles.map(s => (
                <button
                  key={s.id}
                  onClick={() => setSelectedStyle(s.id)}
                  className={`px-3 py-1.5 rounded-full border text-sm transition-all ${
                    selectedStyle === s.id
                      ? 'border-purple-500 bg-purple-500 text-white'
                      : 'border-slate-200 hover:border-slate-300'
                  }`}
                >
                  {s.name}
                </button>
              ))}
            </div>
          </div>

          <div className="mb-5">
            <label className="text-sm font-medium text-slate-700 mb-2 block">
              生成数量：{imageCount} 张
            </label>
            <input
              type="range"
              min="1"
              max="8"
              value={imageCount}
              onChange={(e) => setImageCount(parseInt(e.target.value))}
              className="w-full accent-purple-500"
            />
          </div>

          <div className="mb-5">
            <label className="text-sm font-medium text-slate-700 mb-2 block">描述词（Prompt）</label>
            <textarea
              value={prompt}
              onChange={(e) => setPrompt(e.target.value)}
              placeholder="例如：一只戴墨镜的柴犬在海浪上冲浪，卡通风格，明亮的色彩"
              className="w-full p-3 border border-slate-200 rounded-lg text-sm h-24 resize-none focus:outline-none focus:border-purple-500"
            />
            <div className="flex gap-2 mt-2">
              <button className="text-xs px-2 py-1 bg-slate-100 text-slate-600 rounded hover:bg-slate-200">
                ✨ AI优化提示词
              </button>
              <button className="text-xs px-2 py-1 bg-slate-100 text-slate-600 rounded hover:bg-slate-200">
                🌐 翻译为英文
              </button>
            </div>
          </div>

          <button
            onClick={handleGenerateImages}
            disabled={isGenerating}
            className="w-full bg-gradient-to-r from-purple-500 to-pink-500 text-white font-medium py-3 rounded-lg hover:opacity-90 transition-opacity flex items-center justify-center gap-2 disabled:opacity-50"
          >
            {isGenerating ? (
              <>
                <RefreshCw className="w-4 h-4 animate-spin" />
                生成中...
              </>
            ) : (
              <>
                <Sparkles className="w-4 h-4" />
                生成图案
              </>
            )}
          </button>
          <div className="text-xs text-slate-500 text-center mt-2">
            预估消耗：约 ¥{(imageCount * 0.5).toFixed(2)}
          </div>
        </div>

        <div className="col-span-2">
          <div className="bg-white rounded-xl border border-slate-200 p-6 min-h-full">
            <div className="flex items-center justify-between mb-4">
              <h3 className="font-bold text-slate-800">生成结果</h3>
              {generatedImages.length > 0 && (
                <div className="flex gap-2">
                  <button className="text-sm px-3 py-1.5 border border-slate-200 rounded-lg hover:bg-slate-50">
                    全部下载
                  </button>
                  <button className="text-sm px-3 py-1.5 bg-purple-500 text-white rounded-lg hover:bg-purple-600">
                    保存到素材库
                  </button>
                </div>
              )}
            </div>

            {generatedImages.length === 0 && !isGenerating && (
              <div className="flex flex-col items-center justify-center py-20 text-slate-400">
                <ImageIcon className="w-16 h-16 mb-3" />
                <p>左侧设置参数后，点击生成图案</p>
              </div>
            )}

            {isGenerating && (
              <div className="grid grid-cols-2 gap-4">
                {Array.from({ length: imageCount }).map((_, i) => (
                  <div key={i} className="aspect-square bg-slate-100 rounded-lg animate-pulse flex items-center justify-center">
                    <Sparkles className="w-8 h-8 text-slate-300" />
                  </div>
                ))}
              </div>
            )}

            {generatedImages.length > 0 && !isGenerating && (
              <div className="grid grid-cols-2 gap-4">
                {generatedImages.map((img, i) => (
                  <div key={img.id} className="group relative">
                    <div className="aspect-square bg-gradient-to-br from-purple-100 via-pink-100 to-orange-100 rounded-lg overflow-hidden flex items-center justify-center">
                      <div className="text-6xl">🎨</div>
                    </div>
                    <div className="absolute inset-0 bg-black bg-opacity-50 opacity-0 group-hover:opacity-100 transition-opacity rounded-lg flex items-center justify-center gap-2">
                      <button className="p-2 bg-white rounded-lg hover:bg-slate-100">
                        <Download className="w-4 h-4" />
                      </button>
                      <button className="p-2 bg-white rounded-lg hover:bg-slate-100">
                        <Heart className="w-4 h-4" />
                      </button>
                      <button className="p-2 bg-white rounded-lg hover:bg-slate-100">
                        <RefreshCw className="w-4 h-4" />
                      </button>
                    </div>
                    <div className="mt-2 text-xs text-slate-500">变体 #{i + 1}</div>
                  </div>
                ))}
              </div>
            )}
          </div>
        </div>
      </div>
    </div>
  );

  const renderTitleGeneration = () => (
    <div className="p-8">
      <div className="mb-6">
        <h1 className="text-2xl font-bold text-slate-800">标题生成</h1>
        <p className="text-slate-500 mt-1">批量生成符合电商平台SEO规则的产品标题</p>
      </div>

      <div className="grid grid-cols-3 gap-6">
        <div className="col-span-1 bg-white rounded-xl border border-slate-200 p-6 h-fit">
          <h3 className="font-bold text-slate-800 mb-4">生成参数</h3>

          <div className="mb-5">
            <label className="text-sm font-medium text-slate-700 mb-2 block">产品类型</label>
            <select className="w-full p-2.5 border border-slate-200 rounded-lg text-sm focus:outline-none focus:border-blue-500">
              <option>T恤 (T-Shirt)</option>
              <option>马克杯 (Mug)</option>
              <option>手机壳 (Phone Case)</option>
              <option>帆布包 (Tote Bag)</option>
              <option>抱枕 (Pillow)</option>
              <option>卫衣 (Hoodie)</option>
            </select>
          </div>

          <div className="mb-5">
            <label className="text-sm font-medium text-slate-700 mb-2 block">目标市场</label>
            <div className="grid grid-cols-2 gap-2">
              {[
                { id: 'us', name: '🇺🇸 美国' },
                { id: 'uk', name: '🇬🇧 英国' },
                { id: 'eu', name: '🇪🇺 欧洲' },
                { id: 'jp', name: '🇯🇵 日本' }
              ].map(m => (
                <button
                  key={m.id}
                  onClick={() => setSelectedMarket(m.id)}
                  className={`p-2.5 rounded-lg border text-sm transition-all ${
                    selectedMarket === m.id
                      ? 'border-blue-500 bg-blue-50 text-blue-700'
                      : 'border-slate-200 hover:border-slate-300'
                  }`}
                >
                  {m.name}
                </button>
              ))}
            </div>
          </div>

          <div className="mb-5">
            <label className="text-sm font-medium text-slate-700 mb-2 block">风格语气</label>
            <div className="flex flex-wrap gap-2">
              {['专业', '俏皮', '简约', '促销', '节日'].map(t => (
                <button key={t} className="px-3 py-1.5 rounded-full border border-slate-200 text-sm hover:border-blue-500 hover:bg-blue-50">
                  {t}
                </button>
              ))}
            </div>
          </div>

          <div className="mb-5">
            <label className="text-sm font-medium text-slate-700 mb-2 block">
              生成数量：{titleCount} 条
            </label>
            <input
              type="range"
              min="5"
              max="30"
              value={titleCount}
              onChange={(e) => setTitleCount(parseInt(e.target.value))}
              className="w-full accent-blue-500"
            />
          </div>

          <div className="mb-5">
            <label className="text-sm font-medium text-slate-700 mb-2 block">关键词</label>
            <textarea
              value={titlePrompt}
              onChange={(e) => setTitlePrompt(e.target.value)}
              placeholder="例如：surfing dog, funny, summer beach"
              className="w-full p-3 border border-slate-200 rounded-lg text-sm h-20 resize-none focus:outline-none focus:border-blue-500"
            />
          </div>

          <div className="mb-5">
            <label className="text-sm font-medium text-slate-700 mb-2 block">字符长度限制</label>
            <select className="w-full p-2.5 border border-slate-200 rounded-lg text-sm">
              <option>Temu标准 (≤150字符)</option>
              <option>Amazon标准 (≤200字符)</option>
              <option>自定义长度</option>
            </select>
          </div>

          <button
            onClick={handleGenerateTitles}
            disabled={isGeneratingTitles}
            className="w-full bg-gradient-to-r from-blue-500 to-cyan-500 text-white font-medium py-3 rounded-lg hover:opacity-90 transition-opacity flex items-center justify-center gap-2"
          >
            {isGeneratingTitles ? (
              <>
                <RefreshCw className="w-4 h-4 animate-spin" />
                生成中...
              </>
            ) : (
              <>
                <Zap className="w-4 h-4" />
                生成标题
              </>
            )}
          </button>
        </div>

        <div className="col-span-2">
          <div className="bg-white rounded-xl border border-slate-200 p-6">
            <div className="flex items-center justify-between mb-4">
              <h3 className="font-bold text-slate-800">生成结果</h3>
              {generatedTitles.length > 0 && (
                <div className="flex gap-2">
                  <button className="text-sm px-3 py-1.5 border border-slate-200 rounded-lg hover:bg-slate-50 flex items-center gap-1">
                    <Copy className="w-3.5 h-3.5" />
                    全部复制
                  </button>
                  <button className="text-sm px-3 py-1.5 border border-slate-200 rounded-lg hover:bg-slate-50 flex items-center gap-1">
                    <Download className="w-3.5 h-3.5" />
                    导出CSV
                  </button>
                  <button className="text-sm px-3 py-1.5 bg-blue-500 text-white rounded-lg hover:bg-blue-600">
                    保存到素材库
                  </button>
                </div>
              )}
            </div>

            {generatedTitles.length === 0 && !isGeneratingTitles && (
              <div className="flex flex-col items-center justify-center py-20 text-slate-400">
                <Type className="w-16 h-16 mb-3" />
                <p>设置参数后，点击生成标题</p>
              </div>
            )}

            {isGeneratingTitles && (
              <div className="space-y-2">
                {Array.from({ length: 5 }).map((_, i) => (
                  <div key={i} className="h-12 bg-slate-100 rounded-lg animate-pulse"></div>
                ))}
              </div>
            )}

            {generatedTitles.length > 0 && !isGeneratingTitles && (
              <div className="space-y-2">
                {generatedTitles.map((title, i) => (
                  <div key={title.id} className="flex items-center gap-3 p-3 border border-slate-200 rounded-lg hover:border-blue-300 hover:bg-blue-50 transition-all group">
                    <div className="w-7 h-7 rounded-full bg-slate-100 text-slate-600 text-xs flex items-center justify-center font-medium flex-shrink-0">
                      {i + 1}
                    </div>
                    <div className="flex-1 text-sm text-slate-800">{title.text}</div>
                    <div className="text-xs text-slate-400">{title.text.length}字符</div>
                    <button className="opacity-0 group-hover:opacity-100 p-1.5 hover:bg-blue-100 rounded transition-all">
                      <Copy className="w-4 h-4 text-blue-600" />
                    </button>
                    <button className="opacity-0 group-hover:opacity-100 p-1.5 hover:bg-red-100 rounded transition-all">
                      <Trash2 className="w-4 h-4 text-red-500" />
                    </button>
                  </div>
                ))}
              </div>
            )}
          </div>
        </div>
      </div>
    </div>
  );

  const renderLibrary = () => (
    <div className="p-8">
      <div className="mb-6 flex items-center justify-between">
        <div>
          <h1 className="text-2xl font-bold text-slate-800">素材库</h1>
          <p className="text-slate-500 mt-1">管理已生成的图案和标题，导出对接工厂</p>
        </div>
        <div className="flex gap-2">
          <button className="px-4 py-2 border border-slate-200 rounded-lg text-sm hover:bg-slate-50 flex items-center gap-2">
            <Download className="w-4 h-4" />
            批量导出
          </button>
          <button className="px-4 py-2 bg-purple-500 text-white rounded-lg text-sm hover:bg-purple-600 flex items-center gap-2">
            <Plus className="w-4 h-4" />
            新建分组
          </button>
        </div>
      </div>

      <div className="bg-white rounded-xl border border-slate-200 p-4 mb-6">
        <div className="flex items-center gap-3">
          <div className="flex-1 relative">
            <Search className="w-4 h-4 absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" />
            <input
              type="text"
              placeholder="搜索图案、标题、关键词..."
              className="w-full pl-9 pr-4 py-2 border border-slate-200 rounded-lg text-sm focus:outline-none focus:border-purple-500"
            />
          </div>
          <select className="px-3 py-2 border border-slate-200 rounded-lg text-sm">
            <option>全部产品</option>
            <option>T恤</option>
            <option>马克杯</option>
            <option>手机壳</option>
          </select>
          <select className="px-3 py-2 border border-slate-200 rounded-lg text-sm">
            <option>全部状态</option>
            <option>已上架</option>
            <option>待审核</option>
            <option>草稿</option>
          </select>
          <button className="px-3 py-2 border border-slate-200 rounded-lg text-sm hover:bg-slate-50 flex items-center gap-1">
            <Filter className="w-4 h-4" />
            高级筛选
          </button>
        </div>
      </div>

      <div className="flex gap-2 mb-4">
        {['全部 (3287)', '我的收藏 (124)', '本周新增 (89)', '已上架 (892)', '待审核 (45)'].map((tab, i) => (
          <button
            key={i}
            className={`px-4 py-2 rounded-lg text-sm ${
              i === 0 ? 'bg-purple-500 text-white' : 'bg-white border border-slate-200 hover:bg-slate-50'
            }`}
          >
            {tab}
          </button>
        ))}
      </div>

      <div className="grid grid-cols-4 gap-4">
        {Array.from({ length: 12 }).map((_, i) => (
          <div key={i} className="bg-white rounded-xl border border-slate-200 overflow-hidden hover:shadow-lg transition-shadow group">
            <div className="aspect-square bg-gradient-to-br from-purple-100 via-pink-100 to-orange-100 flex items-center justify-center relative">
              <div className="text-6xl">{['🎨', '🌊', '🐕', '☕', '🌸', '🎸', '🚀', '🌈', '⚡', '🔥', '💎', '🎯'][i]}</div>
              <div className="absolute top-2 right-2 flex gap-1">
                <button 
                  onClick={() => setSavedItems(savedItems.includes(i) ? savedItems.filter(x => x !== i) : [...savedItems, i])}
                  className="w-7 h-7 bg-white bg-opacity-90 rounded-full flex items-center justify-center hover:bg-opacity-100"
                >
                  <Heart className={`w-3.5 h-3.5 ${savedItems.includes(i) ? 'fill-red-500 text-red-500' : 'text-slate-600'}`} />
                </button>
              </div>
              <div className="absolute top-2 left-2">
                <span className={`text-xs px-2 py-0.5 rounded-full ${
                  i % 3 === 0 ? 'bg-green-500 text-white' : 
                  i % 3 === 1 ? 'bg-yellow-500 text-white' : 
                  'bg-slate-500 text-white'
                }`}>
                  {i % 3 === 0 ? '已上架' : i % 3 === 1 ? '待审核' : '草稿'}
                </span>
              </div>
            </div>
            <div className="p-3">
              <div className="text-sm font-medium text-slate-800 truncate">
                设计 #{1000 + i}
              </div>
              <div className="text-xs text-slate-500 truncate mt-1">
                Funny Surfing Dog T-Shirt Design
              </div>
              <div className="flex items-center justify-between mt-2 text-xs text-slate-400">
                <span>👕 T恤</span>
                <span>2小时前</span>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  );

  const renderSettings = () => (
    <div className="p-8">
      <div className="mb-6">
        <h1 className="text-2xl font-bold text-slate-800">系统设置</h1>
        <p className="text-slate-500 mt-1">管理API密钥、团队成员和系统配置</p>
      </div>

      <div className="grid grid-cols-3 gap-6">
        <div className="col-span-2 space-y-6">
          <div className="bg-white rounded-xl border border-slate-200 p-6">
            <h3 className="font-bold text-slate-800 mb-4">API配置</h3>
            <div className="space-y-3">
              {engines.map(e => (
                <div key={e.id} className="flex items-center justify-between p-3 border border-slate-200 rounded-lg">
                  <div>
                    <div className="font-medium text-slate-800">{e.name}</div>
                    <div className="text-xs text-slate-500">sk-...****1234</div>
                  </div>
                  <div className="flex items-center gap-3">
                    <span className="text-xs px-2 py-1 bg-green-100 text-green-700 rounded-full flex items-center gap-1">
                      <Check className="w-3 h-3" />
                      已连接
                    </span>
                    <button className="text-sm text-purple-600 hover:underline">编辑</button>
                  </div>
                </div>
              ))}
            </div>
          </div>

          <div className="bg-white rounded-xl border border-slate-200 p-6">
            <div className="flex items-center justify-between mb-4">
              <h3 className="font-bold text-slate-800">团队成员</h3>
              <button className="text-sm px-3 py-1.5 bg-purple-500 text-white rounded-lg flex items-center gap-1">
                <Plus className="w-4 h-4" />
                邀请成员
              </button>
            </div>
            <div className="space-y-2">
              {[
                { name: '张三', role: '管理员', email: 'zhangsan@company.com' },
                { name: '李四', role: '设计师', email: 'lisi@company.com' },
                { name: '王五', role: '运营', email: 'wangwu@company.com' }
              ].map((u, i) => (
                <div key={i} className="flex items-center gap-3 p-3 border border-slate-200 rounded-lg">
                  <div className="w-9 h-9 bg-gradient-to-br from-purple-400 to-pink-400 rounded-full flex items-center justify-center text-white font-medium">
                    {u.name[0]}
                  </div>
                  <div className="flex-1">
                    <div className="font-medium text-slate-800">{u.name}</div>
                    <div className="text-xs text-slate-500">{u.email}</div>
                  </div>
                  <span className="text-xs px-2 py-1 bg-slate-100 text-slate-700 rounded">{u.role}</span>
                </div>
              ))}
            </div>
          </div>
        </div>

        <div className="space-y-6">
          <div className="bg-white rounded-xl border border-slate-200 p-6">
            <h3 className="font-bold text-slate-800 mb-4">本月用量</h3>
            <div className="space-y-4">
              <div>
                <div className="flex justify-between text-sm mb-1">
                  <span className="text-slate-600">图片生成</span>
                  <span className="font-medium">2,156 / 5,000</span>
                </div>
                <div className="w-full bg-slate-100 rounded-full h-2">
                  <div className="bg-purple-500 h-2 rounded-full w-2/5"></div>
                </div>
              </div>
              <div>
                <div className="flex justify-between text-sm mb-1">
                  <span className="text-slate-600">文本生成</span>
                  <span className="font-medium">8,420 / 20,000</span>
                </div>
                <div className="w-full bg-slate-100 rounded-full h-2">
                  <div className="bg-blue-500 h-2 rounded-full w-2/5"></div>
                </div>
              </div>
              <div>
                <div className="flex justify-between text-sm mb-1">
                  <span className="text-slate-600">存储空间</span>
                  <span className="font-medium">12.4GB / 50GB</span>
                </div>
                <div className="w-full bg-slate-100 rounded-full h-2">
                  <div className="bg-green-500 h-2 rounded-full w-1/4"></div>
                </div>
              </div>
            </div>
          </div>

          <div className="bg-gradient-to-br from-purple-500 to-pink-500 rounded-xl p-6 text-white">
            <h3 className="font-bold mb-2">需要更多资源？</h3>
            <p className="text-sm text-purple-100 mb-4">联系IT管理员升级套餐</p>
            <button className="w-full bg-white text-purple-600 font-medium py-2 rounded-lg hover:bg-purple-50">
              申请扩容
            </button>
          </div>
        </div>
      </div>
    </div>
  );

  return (
    <div className="flex bg-slate-50 min-h-screen">
      {renderSidebar()}
      <div className="flex-1 overflow-auto">
        <div className="bg-white border-b border-slate-200 px-8 py-3 flex items-center justify-between">
          <div className="flex items-center gap-2 text-sm text-slate-500">
            <span>POD AI Studio</span>
            <ChevronRight className="w-3 h-3" />
            <span className="text-slate-800 font-medium">
              {currentPage === 'dashboard' && '工作台'}
              {currentPage === 'image' && '图案生成'}
              {currentPage === 'title' && '标题生成'}
              {currentPage === 'library' && '素材库'}
              {currentPage === 'settings' && '系统设置'}
            </span>
          </div>
          <div className="flex items-center gap-3">
            <button className="p-2 hover:bg-slate-100 rounded-lg relative">
              <Bell className="w-4 h-4 text-slate-600" />
              <span className="absolute top-1 right-1 w-2 h-2 bg-red-500 rounded-full"></span>
            </button>
            <div className="flex items-center gap-2 px-3 py-1.5 hover:bg-slate-100 rounded-lg cursor-pointer">
              <div className="w-7 h-7 bg-gradient-to-br from-purple-400 to-pink-400 rounded-full flex items-center justify-center text-white text-xs font-medium">
                我
              </div>
              <span className="text-sm text-slate-700">张三</span>
              <ChevronDown className="w-3 h-3 text-slate-500" />
            </div>
          </div>
        </div>
        {currentPage === 'dashboard' && renderDashboard()}
        {currentPage === 'image' && renderImageGeneration()}
        {currentPage === 'title' && renderTitleGeneration()}
        {currentPage === 'library' && renderLibrary()}
        {currentPage === 'settings' && renderSettings()}
      </div>
    </div>
  );
};

export default PODPlatform;