name: Deploy to GitHub Pages

on:
  push:
    branches: [main, master]
    paths:
      - 'temu-dashboard/temu-dashboard.html'
      - 'temu-dashboard/temu-dashboard-editorial.html'
      - 'temu-dashboard/docs/**'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Prepare Pages site
        run: |
          mkdir -p docs
          # 优先 docs/（由 同步Pages.bat 从源文件复制）；缺失时回退根目录源文件
          if [ -f temu-dashboard/docs/index.html ]; then
            cp temu-dashboard/docs/index.html docs/index.html
          else
            cp temu-dashboard/temu-dashboard.html docs/index.html
          fi
          if [ -f temu-dashboard/docs/editorial.html ]; then
            cp temu-dashboard/docs/editorial.html docs/editorial.html
          elif [ -f temu-dashboard/temu-dashboard-editorial.html ]; then
            cp temu-dashboard/temu-dashboard-editorial.html docs/editorial.html
          fi
          for f in 组员使用说明.html OSS与协作版说明.html 先看这里.txt; do
            if [ -f "temu-dashboard/docs/$f" ]; then
              cp "temu-dashboard/docs/$f" "docs/$f"
            elif [ -f "temu-dashboard/$f" ]; then
              cp "temu-dashboard/$f" "docs/$f"
            fi
          done
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
